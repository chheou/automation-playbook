# util-linux: mount/cgroup/symlink issues — no Debian fix published yet
CVE-2026-76642
CVE-2026-78408
CVE-2026-78409
CVE-2026-78410

# libacl: symlink traversal privilege escalation — no fix yet
CVE-2026-54369

# ncurses: buffer overflow — no fix yet
CVE-2025-69720

# systemd-homed: local privilege escalation — no fix yet
CVE-2026-16742

# perl-Archive-Tar: DoS via crafted tar header — fix deferred by Debian
CVE-2026-9538

# setuptools 70.3.0 — same situation as msgpack: fixed version (78.1.1) exists,
# but this is pip's internally bundled copy, not our project's setuptools pin
# (already upgraded separately to 84.0.0, which shows clean)
CVE-2025-47273

# msgpack 1.1.2 — fixed version (1.2.1) exists upstream, but this is pip's
# own internal vendored copy (pip/_vendor/msgpack), not a top-level dependency
# we control. Fix requires the pip project itself to bump its bundled version.
GHSA-6v7p-g79w-8964
